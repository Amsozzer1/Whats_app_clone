import React,{useState,useEffect} from 'react'
import ChatScroll from './ChatScroll';

export default function OnlyChatScroll(
    {chats, handleOnClick,user={user}}
){

    return(
        <div></div>
    );
}